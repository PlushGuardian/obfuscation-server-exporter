name: Build multiarch binaries

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      commit_sha:
        required: true
        type: string
      binary_name:
        required: true
        type: string
      architectures:
        required: true
        type: string

jobs:
  build-binaries:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include: ${{ fromJson(inputs.architectures) }}
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: '1.25.7'

      - name: Build binary
        env:
          CGO_ENABLED: 0
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
          GOARM: ${{ matrix.goarm || '' }}
        run: |
          go build \
            -ldflags="-w -s -X main.version=${{ inputs.version }} -X main.commit=${{ inputs.commit_sha }}" \
            -trimpath \
            -o ${{ binary_name }}-${{ matrix.os }}-${{ matrix.arch }}${{ matrix.os == 'windows' && '.exe' || '' }}

      - name: Archive binary
        run: |
          if [ "${{ matrix.os }}" = "windows" ]; then
            zip ${{ binary_name }}-${{ matrix.os }}-${{ matrix.arch }}.zip ${{ binary_name }}-${{ matrix.os }}-${{ matrix.arch }}.exe
          else
            tar -czf ${{ binary_name }}-${{ matrix.os }}-${{ matrix.arch }}.tar.gz ${{ binary_name }}-${{ matrix.os }}-${{ matrix.arch }}
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ binary_name }}-${{ matrix.os }}-${{ matrix.arch }}
          path: ${{ binary_name }}-${{ matrix.os }}-${{ matrix.arch }}.*
